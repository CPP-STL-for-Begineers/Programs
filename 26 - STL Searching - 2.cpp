#include<bits/stdc++.h>
using namespace std;

int find_largest_num_smaller_than(vector<int> &v, int search) {
   vector<int>::iterator  =  
}

int main() {
    vector<int> v {1, 4, 4, 4, 4, 9, 9, 10, 11};
    for(auto &i: v) cout << i << ", "; cout << " | Size : " << v.size() << endl;
    
    sort(v.begin(), v.end());
    for(auto &i: v) cout << i << ", "; cout << " | Size : " << v.size() << endl;


}